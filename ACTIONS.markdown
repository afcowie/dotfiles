Tasks to initialize a new user
==============================

In Gnome Shell, have Alt-Tab only switch between applications on current
workspace.

    $ gsettings set org.gnome.shell.app-switcher current-workspace-only true
